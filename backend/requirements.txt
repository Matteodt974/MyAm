# --- Noyau API (suffisant pour démarrer / sprint 0) ---
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic-settings>=2.5
python-multipart>=0.0.12   # nécessaire pour les uploads multipart (scan photo)

# --- À DÉCOMMENTER quand  BD + l'auth (sprint 1/2) ---
# sqlalchemy>=2.0           # ORM pour la gestion de la base de données
# psycopg2-binary>=2.9      # driver PostgreSQL pour Python
# alembic>=1.13             # outil de migration de schéma de base de données
# pyjwt[crypto]>=2.9        #authentification JWT (JSON Web Tokens)
# pwdlib[bcrypt]>=0.2       # pour le hachage sécurisé des mots de passe

