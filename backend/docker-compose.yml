services:
  api:
    build: .
    ports: ['127.0.0.1:8545:8080']
    env_file: .env

  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: scan
  #     POSTGRES_PASSWORD: scan
  #     POSTGRES_DB: scan
  #   ports: ['5433:5432']
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  # libretranslate:
  #   image: libretranslate/libretranslate:latest
  #   ports: ['5001:5000']
  #   environment:
  #     LT_LOAD_ONLY: 'en,fr,zh'
  #   volumes:
  #     - lt_models:/home/libretranslate/.local

  # ocr:
  #   build: ./ocr
  #   ports: ['8001:8001']

# volumes:
#   pgdata:
#   lt_models:
